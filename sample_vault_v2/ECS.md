#project #gamedev #architecture

# ECS

Entity-Component-System: data in components, behavior in systems, identity in entities.
A clean fit for a roguelike where everything is "a thing with stats and tags."

Part of the [[Roguelike Project]], implemented in [[Rust]].
