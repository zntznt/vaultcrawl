#project #gamedev

# Procedural Generation

The art of generating fair, surprising content from a seed. Connectivity first
(Delaunay + minimum spanning tree guarantees a path), then decorate. Never let the
decorator touch the guarantees.

![[noise-diagram.png]]

Feeds the [[Roguelike Project]] and leans on [[Rust]].
