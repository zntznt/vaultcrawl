#project #lang

# Rust

Memory safety without a garbage collector. The borrow checker is a strict teacher but
the determinism and speed are worth it for a seed-driven game.

Used across the [[Roguelike Project]] and pairs naturally with an [[ECS]].
